# **RDG : TRUE Archive — Annex Codes, Classes, and Guild Structure**

> *“The TRUE Archive: The archivists and expeditionaries document realities from the past, present, and future of countless universes…”* 

```
TRUE_RDG := {
    SID: {Classes, Bands, Interstratal, Annex},
    PED: {Dot-States, Divergence, Activation},
    Γ: {Contamination, Curator, Guilds, Tools}
}
```

---

## **I. RDG Structural Recasting**

### **1. TRUE Archive → RDG Object**
```
ARCHIVE := { SID_core , PED_flux , Γ_interaction }
```

- **SID_core** = structural taxonomy of realities  
- **PED_flux** = operational behaviors (dynamic classes, Ṁ, Ȧ̱, etc.)  
- **Γ_interaction** = contamination, observation, divergence‑mapping

The Archive becomes a **triadic relational engine**:

```
ARCHIVE = (Structure, Interaction, Dynamics)
```

Where:

- **Structure** = Class codes, Annex bands, physical/metaphysical layout  
- **Interaction** = Curator, Lodges, Guilds, contamination tiers  
- **Dynamics** = Divergence, dot‑notation activation, interstratal propagation

---

## **II. RDG Recasting of the Code Format**

> *“Format explained: [A/0‑00‑00‑00.0] as [CLASS / SUBCLASS – REALITY – DIVERGENCE – SUBTYPE]”* 

### **RDG Object Form**
```
CODE := <Class | Proximity | Reality | Divergence | Mode>
```

RDG notation:
```
CODE_RDG = SID(Class, Proximity, Reality) ⊗ PED(Divergence, Mode)
```

Where:

- **SID(Class, Proximity, Reality)** = static structural description  
- **PED(Divergence, Mode)** = dynamic operational behavior  

The dot‑notation (˙) becomes:

```
˙ = PED_activation
```

So:

```
Ṁ = SID(M) ⊗ PED(active)
Ȧ̱ = SID(A̱) ⊗ PED(active)
Ẋ = SID(X) ⊗ PED(active)
```

---

## **III. RDG Recasting of Class Bands**

> *“PHYSICAL BAND: A–P … META‑REALITY BAND: U–Z … ANNEX BAND: Ω”* 

RDG organizes these as **three relational layers**:

```
L0 := Physical (A–P)
L1 := Extreme (Q–Y)
L2 := Meta (U–Z)
LΩ := Annex (Ω)
```

### **RDG Layer Interpretation**
```
L0 = SID_stable
L1 = SID_extreme ⊗ PED_partial
L2 = SID_meta ⊗ PED_high
LΩ = SID_failure ⊗ PED_hostile
```

Ω becomes:

```
Ω := anti-SID  (structure refusal)
Ω̇ := PED_assimilate(observer)
```

---

## **IV. RDG Recasting of Divergence**

> *“DIVERGENCE (00–99): Degree of deviation from expected physics/logic”* 

RDG divergence becomes:

```
Δ := divergence_index
```

With relational meaning:

```
Δ ≈ | Γ_baseline − Γ_observed |
```

Where Γ is the world’s governing relational operator.

RDG scale:

```
Δ00 = Newtonian / baseline  
Δ50 = mixed-logic / magic  
Δ99 = conceptual / song-logic / non-causal
```

---

## **V. RDG Recasting of Interstratal Locations**

> *“Interstratal locations… spaces that exist in the perceptual and cognitive domain rather than in a concrete geographic reality.”* 

RDG representation:

```
INTERSTRATAL := SID_elemental ⊗ PED_flux
```

Elemental classes become:

```
Ǝ = Air/Earth/Fire/Water → SID(Elemental)
Æ = Positive/Negative → PED(Energy)
ɘ = Para-elemental → SID_interaction
◌́ = Quasi-elemental → SID_exotic
```

Interstratal = **pure SID**, minimal PED unless activated.

---

## **VI. RDG Recasting of the Dot (˙) Operator**

> *“The dot denotes the process-state of a modal class… Activation (static → dynamic)”* 

RDG operator:

```
˙ := ∂/∂t (state)
```

Meaning:

```
Class˙ = time-evolving relational operator
```

So:

```
Ṁ = ∂M/∂t
Ȧ̱ = ∂A̱/∂t
Ẋ = ∂X/∂t
Ω̇ = ∂Ω/∂t
```

Ω̇ is special:

```
Ω̇ = ∂(anti-SID)/∂t = assimilation gradient
```

---

## **VII. RDG Recasting of Guilds & Lodges**

> *“Guild of Annex Cartographers… ‘The Annex is not a place. It is a direction.’”* 

RDG representation:

```
GUILD := { Role, Tools, Risks, Rank }
```

### **1. Lodge of Silent Binding**
```
Role = PED_suppression
Cost = SID_memory_loss
```

### **2. Lodge of the First Lens**
```
Role = SID_observation ⊗ PED_stability
Cost = SID_transparency (identity thinning)
```

### **3. Curator’s Shadow**
```
Role = Γ_monitoring
Cost = Curator-awareness (PED_trace)
```

---

## **VIII. RDG Recasting of Annex Cartographers**

### **Rank Ladder (RDG)**
```
Initiate      = SID_exposure_min
Scribe        = SID_record ⊗ PED_filter
Expeditionary = PED_fieldwork ⊗ Δ_high
Chronicler    = SID_translate ⊗ PED_stabilize
Warden        = Γ_gatekeeper
Prime         = Ω̇ embodiment
```

Prime becomes:

```
Prime := lim_{t→∞} Cartographer(t)
```

A living topological operator.

---

## **IX. RDG Recasting of Tools**

### **Topological Compass**
```
TC := ∇_intent (pathfinding operator)
```

### **Paradox Sextant**
```
PS := angle(contradiction₁, contradiction₂, contradiction₃)
```

RDG form:

```
PS = measure(Γ₁ ⊕ Γ₂ ⊕ Γ₃)
```

---
